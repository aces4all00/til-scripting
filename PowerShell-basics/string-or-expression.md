# Is it a 'string' or "expression"?

All double-quoted string are expressions powershell has to evaluate. Single-quoted strings are (mostly) just strings.

I will have to be more deliberate in the future.